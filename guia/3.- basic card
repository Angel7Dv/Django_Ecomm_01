
-Create a footer and move "Contact" link down about page
-Make it possible to set ordering for the categories

    class Category(models.Model):
        .....
        ordering = models.IntegerField(default=0)
        class Meta:
            ordering = ('ordering',) #la variable es el parametro del admin, y el string es nuestro campo, se usa la coma para que detecte una tupla
            
-Add ordering to products, newest first


    class Product(models.Model):
        ...
        ...    
        #Ordenar por fecha
        date_add = models.DateTimeField(auto_now_add=True)

        class Meta:
            ordering = ('-date_add',)



-Only show featured products on the frontpage


